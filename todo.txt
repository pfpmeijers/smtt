- Remove requirement that referred states of other state machines need to exist explicitly in a separate
  *.state-machine.md file. Instead, when there is no state machine for a state defined explicitly, 
  then the state machine can be considered anonymous. 

- Add a smtt simplify function that generates a minimized state machine md file, based on an fully 
  completed/normalized AST. The minimize function therefore first rigidly validates the input AST.

- Add to spec that example values can be taken from other state machines.

- ISSUE:
  The "other" data values are moved to the example data values table. But now there are scenarios with examples
  that are not really needed. A 2nd value just added to serve a "different" modifier for a specific scenario.
  Use () between orginally "other" values to indicate these can be skipped unless needed for this modifier.
  So you would get:
      | email address       |
      |---------------------|
      | info@domain.com     |
      | (other@example.com) |
  This is not completely clean because "other" needs to be specified per row and not cell.
  Note that overhead of running second scenario case is minimal. So why bother ...

- Add to the validate step: detect transitions within a state machine that share the same effective
  precondition state(s) and the same trigger but produce different results (ambiguous transitions).
  E.g. reservation-form.state-machine.md rules 052-054 (generic single-attribute preconditions)
  overlapped with rules 055-057 (more specific two-attribute preconditions) for the same trigger.

- C:\Users\pfpme\WebstormProjects\smtt>node "src/generate/tests/utils/coverage-report.cjs"
  Test count: 108
  Total requirements: 157
  Covered: 156
  Uncovered: 168
  Claimed requirements not in spec: 201, 202, 203, 204, 205, 206, 207, 208, 210, 211, 214, 215, 216, 217, 218, 219, 220, 226, 227, 228, 229, 230, 231, 232, 233, 235, 306, 307, 308, 309, 310, 312, 313, 315, 316, 317, 318, 319, 320, 322
  Tests without requirements:
  Unused test IDs:

- Add validation for duplicate transition ID.

- Let the ohm grammar already distinguish between numerical and string attr. Strings have "".
- Use null for undefined (numerical or string) attr. Null is also allowed in json
- cleanup/compact the sm.spec.md. There is a lot of repetition that makes it very verbose and hard to read.
- Consider following: Only if result is set for attr that is also used in precond/trigger, then add "resulting" prefix.
     Otherwise it might look strange/unneeded. But check the real cases where this happens first.


- Some comments are much elaborating on what is -not- is (no longer). Remove that. Just describe info about what it is.

- Consider line breaks in state table. Like below.
    | #        | States                                          | Trigger                                            | Result                                                          | Notes                                                                       |
    |==========|=================================================|====================================================|=================================================================|=============================================================================|
    | 038a     | `User session present`,                         | `Painting added to cart`                           | `Painting in cart`                                              | Adding painting to cart, while user not authenticated.                      |
    |          | `User unauthenticated`,                         |                                                    | with `painting email address` undefined                         |                                                                             |
    |          | `Painting available`                            |                                                    |                                                                 |                                                                             |
    | -------  | ----------------------------------------------  | -------------------------------------------------  |-----------------------------------------------------------------| --------------------------------------------------------------------------  |
    | 038b     | `User session present`,                         | `Painting added to cart`                           | `Painting in cart`                                              | Adding painting to cart.                                                    |
    |          | `User authenticated` as `user email address`,   | `painting email address` as `user email address`   |                                                                 |                                                                             |                                                                           |
    |          | `Painting available`                            |                                                    |                                                                 |                                                                             |                                                                           |
    | -------- | ----------------------------------------------- | -------------------------------------------------- |-----------------------------------------------------------------| --------------------------------------------------------------------------- |
    | 039a     | `User session present`,                         | `Painting reservation confirmed`                   | `Painting reserved`                                             | Reservation confirmed. User implicitly authenticated by reservation.        |
    |          | `User unauthenticated`,                         | using `reservation email address`                  | for `reservation email address`                                 |                                                                             |
    |          | `Painting in cart`                              |                                                    | and `painting email address` set to `reservation email address` |                                                                             |
    |          | `painting email address` undefined              |                                                    |                                                                 |                                                                             |                                                                          |
    | -------- | ----------------------------------------------- | -------------------------------------------------- |-----------------------------------------------------------------| --------------------------------------------------------------------------- |
    | 042      | `User session present`,                         | `Painting removed from cart`                       | `Painting available`                                            | Removing painting from cart.                                                |
    |          | `User authenticated`,                           |                                                    |                                                                 |                                                                             |
    |          | `Painting in cart`                              |                                                    |

  Or better, allow alternative bulleted definition:

      - 039c:
        - States:
          - `User session present`
          - `User authenticated` with `user email address`
          - `Painting in cart` with `painting assignee email address` as `user email address`
        - Trigger:
          - `Painting reservation confirmed` using
            `reservation email address` <> `user email address` and `reservation name`
        - Result:
          - `Painting reserved` for `reservation email address` and `reservation name`
             and `painting email address` set to `reservation email address`
        - Notes:
          - Reservation confirmed using new email address


- Implied states
    E.g. `user profile unavailable` implies `user unauthenticated`
         `(un)named profile available` with `profile email address`: `user authenticated` with `user email address` as `profile email address`

- Make attr "name space" prefix optional/intelligent.
    E.g. instead of implication:
       `named profile available` with `profile email address`: `user authenticated` with `user email address` as `profile email address`
    Specify:
       `named profile available` with its `email address`: `user authenticated` with its `email address` as `profile email address`
    And then at sm level specify namespace that acts as prefix.
        # User profile
        Namespace: profile // To prefix attributes

- Add to AST validation:
  - Check that all identifiers are properly matched to owning state machine and no duplicates.
  - If state arguments are different then it should be checked whether they are not disjunct.
    E.g. a = 1 and a = 2
  - smtt.generate.features.md REQ-117 ("when two references share the
    same state name but carry different arguments, both shall be kept") may be
    an invalid requirement — the same state name with different arguments in
    the same Given list looks like a contradiction rather than a legitimate
    case. Confirm whether this scenario can genuinely occur and, if not, remove
    REQ-117 (and dedupeStateRefs' handling of it) from the spec/generator;
    otherwise fix the wording/example and re-add a proper test case
    (previously TST-052, removed from state-trigger-expansion.test.ts pending
    this review).
  - A modifier is only valid whenever a base reference to the same attribute exists.
    It's invalid when there's no prior reference to derive from.
    In other words, When attribute modifier used then there must also be a reference to unmodified attribute.
    The latter using the base column values and former the extra column with modified attribute value.
  - Arguments used without example values defined.
  - Transition precondition states can be duplicated or from same state machine. That should not be allowed. Checker needed for that.
    Default preconditions shall only be for external states.
  - Transition triggers can not be triggers of own state. Only state from other state machines.
  - missing preconditions
  - missing states (e.g. when used in preconditions)
  - if only single example, then 'not' modifier (or others as well) will not work. Raise error.
  - Dependency validation. Check all external states used to have corresponding dep to machine.


- In smtt.*.md req docs, Keep every requirement line short. A first single sentence that in principle covers the
  essence in an unambiguous way. Then after that, add Rationale: ..., Remarks: ... and Examples: ... where applicable.
  Then the annotations in the test results just repeat this first req line instead of the full text.

- Use following (by example) as section #:
      # State machine
      `User authentication`: Indicates whether the user is authenticated (signed in) or not.

      Users are identified by email address only.
      No passwords or other credentials are used.

- Remove explicit initial state statement. Always first state

- Update the test coverage report creation to all current smtt.**.md files.

- Move data before states section since implied conditions rely on data.

- Allow plain text in a section to be just informational text. All real content in bulleted items or tables.

