## Rules

- For state names, try to keep part of the name common between the state 
  variants. So e.g. when a first state called `*standard* view` then 
  do not call a 2nd state `viewing *collage*` but rather `*collage* view`.

- Do NOT add notes at the end of the definition spec that actually contain
  transition information. Then add an explicit transition definition instead.

