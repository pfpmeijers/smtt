# Renumber scripts

This directory contains logic for rewriting transition identifiers in state machine source files.

## Files

- [`renumber.ts`](./renumber.ts): Parses state machine files and rewrites transition ID columns in transition tables.
- [`tests/`](./tests/): Contains regression tests for renumbering behavior.

