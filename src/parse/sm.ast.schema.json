{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://example.com/state-machines.schema.json",
  "title": "State Machines",
  "description": "JSON Schema for the AST output generated by the state machine parser.",
  "type": "array",
  "items": {
    "$ref": "#/definitions/StateMachine"
  },
  "definitions": {
    "StateMachine": {
      "type": "object",
      "description": "The full representation of a parsed *.state-machine.md file.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The title of the state machine (from the H1 heading)."
        },
        "overview": {
          "type": ["string", "null"],
          "description": "A brief description of the state machine's purpose."
        },
        "source": {
          "type": ["string", "null"],
          "description": "The path to the source markdown file, relative to the project root's parent directory."
        },
        "states": {
          "type": "array",
          "description": "A list of all valid states for this machine.",
          "minItems": 1,
          "items": {
            "$ref": "#/definitions/StateDefinition"
          }
        },
        "initialState": {
          "type": "string",
          "description": "The state in which the machine starts."
        },
        "data": {
          "type": "object",
          "description": "Data attributes associated with the state machine. Keyed by attribute name, mapping to the attribute description.",
          "additionalProperties": {
            "type": "string"
          }
        },
        "dataExampleValues": {
          "type": "array",
          "description": "Examples of data combinations, either synthesized from inline examples or explicitly defined in a combination table. By schema convention, an empty string value encodes an undefined/absent value for that attribute.",
          "items": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            }
          }
        },
        "defaultPreconditions": {
          "type": "array",
          "description": "Default precondition states declared in the Transitions section under 'Default preconditions:'. Each entry is a state name whose owning machine is resolved by matching the name against the states of all parsed machines. The state applies as an implicit leading precondition to every transition that does not already mention a state from the same machine.",
          "items": {
            "$ref": "#/definitions/DefaultPrecondition"
          }
        },
        "transitions": {
          "type": "array",
          "description": "Optional transition table defining how the machine moves between states based on triggers. When omitted, this is equivalent to an empty array.",
          "items": {
            "$ref": "#/definitions/Transition"
          }
        },
        "impossible": {
          "description": "Impossible trigger and state combinations, grouped by origin.",
          "$ref": "#/definitions/ImpossibleSection"
        },
        "irrelevant": {
          "type": "array",
          "description": "Explicitly declared combinations where triggers produce the same outcome regardless of the listed state.",
          "items": {
            "$ref": "#/definitions/IrrelevantTrigger"
          }
        }
      },
      "required": [
        "name",
        "states"
      ],
      "additionalProperties": false
    },
    "DefaultPrecondition": {
      "type": "object",
      "description": "A single default precondition state entry, used in the 'Default preconditions:' block of a Transitions section.",
      "properties": {
        "state": {
          "type": "string",
          "description": "The plain text name of the default precondition state."
        },
        "arguments": {
          "type": "array",
          "description": "Optional data attributes associated with this default precondition state. Inherited from the markdown `Default preconditions:` block when the entry carries inline backtick arguments.",
          "items": {
            "$ref": "#/definitions/Argument"
          }
        },
        "description": {
          "type": "string",
          "description": "Optional description providing additional info about the default precondition."
        }
      },
      "required": ["state"],
      "additionalProperties": false
    },
    "StateDefinition": {
      "type": "object",
      "description": "A state belonging to this state machine.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The plain text name of the state, with emphasis markup removed."
        },
        "description": {
          "type": ["string", "null"],
          "description": "Optional description providing additional info about the state."
        },
        "impliedConditions": {
          "type": "array",
          "description": "Data value conditions implied by this state, declared as indented sub-bullets in the state definition.",
          "items": {
            "$ref": "#/definitions/ImpliedCondition"
          }
        },
        "sourceLine": {
          "type": ["integer", "null"],
          "description": "The 1-based line number within the source file defining this state."
        }
      },
      "required": ["name"],
      "additionalProperties": false
    },
    "ImpliedCondition": {
      "type": "object",
      "description": "A data value condition implied by being in a particular state (from an implied-condition sub-bullet).",
      "properties": {
        "attribute": {
          "type": "string",
          "description": "The data attribute name."
        },
        "condition": {
          "description": "The value condition that holds when the state is active.",
          "$ref": "#/definitions/Condition"
        }
      },
      "required": ["attribute", "condition"],
      "additionalProperties": false
    },
    "Transition": {
      "type": "object",
      "description": "A single transition definition describing how the machine moves between states.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique string identifier for this transition (e.g. '001', 'T01a'). Optional in test fixtures; assigned by the parser or renumber command when missing."
        },
        "states": {
          "type": "array",
          "description": "Optional combined precondition states: external preconditions plus the machine's own precondition state. When omitted, this is equivalent to an empty array.",
          "items": {
            "$ref": "#/definitions/StateRef"
          }
        },
        "trigger": {
          "description": "The event or state-change that initiates the transition.",
          "$ref": "#/definitions/Trigger"
        },
        "result": {
          "description": "The resulting state after the transition.",
          "$ref": "#/definitions/StateRef"
        },
        "notes": {
          "type": "string",
          "description": "Additional context or side effects for the transition. Optional."
        },
        "expansion": {
          "type": "array",
          "description": "Resolved explanations of a state trigger: one entry per chain of source transitions leading from an event trigger to this transition's own trigger. Derived data, recorded by the parse step so a consumer need not repeat the resolution; an AST written by hand may omit it, and a consumer then resolves the trigger itself. Absent on an event-triggered transition. An empty array means the resolution ran and found no source, as opposed to not having run.",
          "items": {
            "$ref": "#/definitions/ExpansionPath"
          }
        },
        "sourceLine": {
          "type": ["integer", "null"],
          "description": "The 1-based line number within the source file defining this transition."
        }
      },
      "required": ["trigger", "result"],
      "additionalProperties": false
    },
    "ExpansionPath": {
      "type": "object",
      "description": "One chain of source transitions explaining a state trigger: the transition that produced the triggering state, the transition that triggered that one, and so on down to the transition carrying the event trigger that starts the chain.",
      "properties": {
        "sources": {
          "type": "array",
          "description": "The chain's source transitions, innermost first: `sources[0]` carries the event trigger that starts the chain and the last entry produces the state this transition's own trigger names. Never empty — a trigger nothing explains has no path at all, rather than a path with no sources.",
          "items": {
            "$ref": "#/definitions/ExpansionSourceRef"
          }
        }
      },
      "required": ["sources"],
      "additionalProperties": false
    },
    "ExpansionSourceRef": {
      "type": "object",
      "description": "Position of a source transition in the AST. The transition is identified by its index within its machine's own `transitions` array, since a transition id is optional; the id travels along only to keep the annotation readable.",
      "properties": {
        "stateMachine": {
          "type": "string",
          "description": "Name of the state machine declaring the source transition."
        },
        "transitionIndex": {
          "type": "integer",
          "minimum": 0,
          "description": "Index of the source transition within its machine's `transitions` array."
        },
        "transitionId": {
          "type": "string",
          "description": "The source transition's id, when it has one. Informational only."
        }
      },
      "required": ["stateMachine", "transitionIndex"],
      "additionalProperties": false
    },
    "StateRef": {
      "type": "object",
      "description": "A reference to a state (either internal or external), potentially with data arguments.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The plain text name of the state."
        },
        "arguments": {
          "type": "array",
          "description": "Optional data attributes associated with this state reference.",
          "items": {
            "$ref": "#/definitions/Argument"
          }
        }
      },
      "required": ["name"],
      "additionalProperties": false
    },
    "Trigger": {
      "type": "object",
      "description": "The event or state-change that initiates a transition.",
      "oneOf": [
        { "$ref": "#/definitions/StateTrigger" },
        { "$ref": "#/definitions/EventTrigger" }
      ]
    },
    "StateTrigger": {
      "type": "object",
      "description": "A trigger that fires when another machine enters a specific state.",
      "properties": {
        "type": {
          "const": "state",
          "description": "Discriminator value identifying this as a state trigger."
        },
        "name": {
          "type": "string",
          "description": "The name of the state."
        },
        "arguments": {
          "type": "array",
          "description": "Optional data attributes associated with this trigger.",
          "items": {
            "$ref": "#/definitions/Argument"
          }
        }
      },
      "required": ["type", "name"],
      "additionalProperties": false
    },
    "EventTrigger": {
      "type": "object",
      "description": "A trigger representing an external event driven by an action.",
      "properties": {
        "type": {
          "const": "event",
          "description": "Discriminator value identifying this as an event trigger."
        },
        "name": {
          "type": "string",
          "description": "The plain text name of the event."
        },
        "arguments": {
          "type": "array",
          "description": "Optional data attributes associated with this trigger.",
          "items": {
            "$ref": "#/definitions/Argument"
          }
        }
      },
      "required": ["type", "name"],
      "additionalProperties": false
    },
    "Argument": {
      "type": "object",
      "description": "A data attribute reference, mapped from backticks in the text, with optional qualifiers, modifiers, a condition, or a result. The `qualifier` property and the `modifier` property are mutually exclusive: an argument uses either `qualifier` alone (simple form) or `modifier` with optional `preQualifier`/`postQualifier` (modified form), never both. The `condition` property and the `result` property are likewise mutually exclusive: a state or trigger argument uses `condition` (a precondition or trigger-constraint filter); a transition result argument uses `result` (the postcondition value or reference it sets) instead.",
      "properties": {
        "qualifier": {
          "type": "string",
          "description": "A qualifying preposition (e.g. 'for', 'as') placed before the backtick reference when no modifier is present. Mutually exclusive with `modifier`."
        },
        "preQualifier": {
          "type": "string",
          "description": "A preposition (e.g. 'for', 'as') preceding the modifier. Only used with `modifier`, not with `qualifier`."
        },
        "modifier": {
          "type": "string",
          "description": "A keyword describing the relationship between this value and the value elsewhere (e.g. 'not', 'different', 'next', 'incremented'). Mutually exclusive with `qualifier`."
        },
        "postQualifier": {
          "type": "string",
          "description": "Text following the modifier, used for sentence structuring. Only used with `modifier`, not with `qualifier`."
        },
        "name": {
          "type": "string",
          "description": "The name of the data attribute being referenced."
        },
        "condition": {
          "description": "A precondition or trigger-constraint value condition constraining the attribute to a specific subset of its possible values. Used on state and trigger arguments; a transition result argument uses `result` instead.",
          "$ref": "#/definitions/Condition"
        },
        "result": {
          "description": "The postcondition value or attribute reference a transition result argument sets on the resulting state's attribute, written `attribute set to value` (or `set to undefined`) in the Result column. Used only on a transition's `result` arguments; a state or trigger argument uses `condition` instead.",
          "$ref": "#/definitions/Result"
        },
        "suffix": {
          "type": "string",
          "description": "Plain words following the backtick argument span (after any post-qualifier), used for human-readable sentence continuations such as 'prefilled' or 'only'."
        }
      },
      "required": ["name"],
      "additionalProperties": false,
      "not": {
        "anyOf": [
          { "required": ["qualifier", "modifier"] },
          { "required": ["condition", "result"] }
        ]
      }
    },
    "Condition": {
      "type": "object",
      "description": "A constraint on the data attribute's value (e.g., '=', '>', 'in range', 'as', 'not as', 'undefined'). The value it compares against is a literal, unless `valueIsReference` marks it as the name of another data attribute.",
      "properties": {
        "operator": {
          "type": "string",
          "enum": [
            "=",
            "<>",
            "<",
            ">",
            "<=",
            ">=",
            "as",
            "not as",
            "in",
            "not in",
            "in range",
            "not in range",
            "undefined",
            "defined"
          ],
          "description": "The condition operator. The 'undefined' operator is unary and carries no value. The 'defined' operator is its unary opposite: it requires a value to be present, without specifying which."
        },
        "value": {
          "description": "The literal value or array of values for the condition. Absent when operator is 'undefined'.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          ]
        },
        "valueIsReference": {
          "type": "boolean",
          "description": "When true, `value` names another data attribute (declared in any state machine in the AST) instead of holding a literal: the condition constrains the attribute to the value that the same `dataExampleValues` row holds for the named attribute, relating two attributes rather than pinning one to a fixed value. Set directly by the grammar: a backticked condition value is a reference, a quoted or numeric one is a literal. Only valid on a scalar comparison operator (`=`, `<>`, `<`, `>`, `<=`, `>=`, `as`, `not as`): the set, range and unary operators compare against fixed literals, which a name standing for another attribute's value cannot provide."
        }
      },
      "required": ["operator"],
      "additionalProperties": false
    },
    "Result": {
      "type": "object",
      "description": "The postcondition value or attribute reference a transition result argument sets on an attribute, written `attribute set to value` (or `set to undefined`) in the Result column. Always an equality assignment — the `set to` phrasing has no other operator, so unlike `Condition`, `Result` carries no `operator` field. An empty object (no `value`) means the attribute is set to undefined.",
      "properties": {
        "value": {
          "type": "string",
          "description": "The literal value, or the name of the referenced attribute (when `valueIsReference` is true), the result sets the attribute to. Absent when the result sets the attribute to undefined."
        },
        "valueIsReference": {
          "type": "boolean",
          "description": "When true, `value` names another data attribute (declared in any state machine in the AST) instead of holding a literal: the attribute takes the value that the same `dataExampleValues` row holds for the named attribute. Set directly by the grammar: a backticked result value is a reference, a quoted or numeric one is a literal. A condition value may be a reference too, as its own `valueIsReference` describes."
        }
      },
      "additionalProperties": false
    },
    "ImpossibleSection": {
      "type": "object",
      "description": "Impossible trigger and state combinations, grouped by origin. The `defined` array holds explicitly declared entries. The optional `inferred` array is populated by the infer step.",
      "properties": {
        "defined": {
          "type": "array",
          "description": "Explicitly declared impossible trigger and state combinations.",
          "items": {
            "$ref": "#/definitions/ImpossibleTrigger"
          }
        },
        "inferred": {
          "type": "array",
          "description": "Impossible trigger and state combinations derived by the infer step, absent when only the parser output is present.",
          "items": {
            "$ref": "#/definitions/ImpossibleTrigger"
          }
        }
      },
      "required": ["defined"],
      "additionalProperties": false
    },
    "ImpossibleTrigger": {
      "type": "object",
      "description": "A row declaring an impossible state-trigger combination.",
      "properties": {
        "states": {
          "type": "array",
          "description": "The preconditions/states that make this combination impossible.",
          "items": {
            "type": "string"
          }
        },
        "trigger": {
          "description": "The trigger for this combination.",
          "$ref": "#/definitions/Trigger"
        }
      },
      "required": ["states", "trigger"],
      "additionalProperties": false
    },
    "IrrelevantTrigger": {
      "type": "object",
      "description": "A row declaring an irrelevant state-trigger combination.",
      "properties": {
        "states": {
          "type": "array",
          "description": "The preconditions/states that make this combination irrelevant.",
          "items": {
            "type": "string"
          }
        },
        "trigger": {
          "description": "The trigger for this combination.",
          "$ref": "#/definitions/Trigger"
        }
      },
      "required": ["states", "trigger"],
      "additionalProperties": false
    }
  }
}
